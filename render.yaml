services:
  - type: web
    name: emit-ai-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python download_pretrained.py
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
